<?xml version="1.0" encoding="UTF-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 陀螺仪按钮 -->
    <item android:state_checked="true"
        android:drawable="@mipmap/panorama_img" /> <!-- 如果是true，运行时显示的是 android:textOn 的值 -->
    <item android:state_checked="false" 
        android:drawable="@mipmap/unpanorama_img" /> <!-- 如果是false，运行是显示的是 android:textOff 的值 -->
</selector>